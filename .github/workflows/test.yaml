name: Test CI Workflow

on:
  push:
    branches:
      - Backend

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            backend

      - name: Dependencies install
        run: |
          echo "Dependencies install process"
          npm install
          echo "Dependencies install success"
